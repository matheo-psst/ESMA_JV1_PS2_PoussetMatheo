Letters += spd;
text_current = string_copy(text,1,floor(letters));

draw_set_front(fSign);
if(h == 0) h = string_height(text);
W = string_width(text_current)


if (letters >= length) && (keyboard_chek_pressed(vk_anykey))
{
	instance_destroy();
	
}